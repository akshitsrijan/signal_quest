export type TimelineEvent = {
  date: string;
  label: string;
};

// Add timeline milestones here, in order, e.g.
// { date: "Aug 1", label: "Registration opens" }.
export const TIMELINE: TimelineEvent[] = [];
